apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-world-app-test
spec:
  replicas: 2 # tells deployment to run 2 pods matching the template
  template: # create pods using pod definition in this template
    metadata:
      # generated from the deployment name
      labels:
        app: hwapp-test
    spec:
      containers:
      - name: hwapp
        image: abhivaidya/hello-world-sprboot
        ports:
        - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: hwapp-svc-test
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: hwapp-test
